package class0.class1;

public class Student {
    // 클래스 = 설계도 -> 객체를 만들기 위한 설계도
    // ※ 객체: 물리적으로 존재하거나 추상적으로 생각할 수 있는 것 중에서 자신의 속성을 가지고 있으면서 식별 가능한 것
    // -> 객체는 속성과 동작으로 구성
    // -> 자바에서는 이러한 속성과 동작을 각각 필드와 메소드라고 부른다.

    // 인스턴스: 설계도 대로 생성된 객체, new 연산자로 생성

    // 1. 필드: 클래스에 정의한 변수, 데이터 항목을 가리키는 용어, 멤버 변수라고 부름
    String name;
    int score;
    int age;

    // 2. 생성자

    // 3. 메서드
}
